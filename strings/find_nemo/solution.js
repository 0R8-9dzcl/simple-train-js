/**
 * Сложность .5
 * Задача: Найти Nemo
 *
 * Напишите функцию, которая ищет подстроку "Nemo" в заданной строке и, если она найдена,
 * возвращает сообщение вида "I found Nemo at [индекс]!". Если "Nemo" не был найден, то
 * верните "I can't find Nemo :(".
 *
 * @param {string} sentence - Входная строка, в которой ищем "Nemo".
 * @returns {string} - Результат поиска "Nemo".
 *
 * Примеры:
 * solution("Hello world");    // Должно вернуть "I can't find Nemo :("
 * solution("Hi Nemo");        // Должно вернуть "I found Nemo at 2!"
 * solution("Nemo James Nemo"); // Должно вернуть "I found Nemo at 0!"
 * solution("Nemo is me");     // Должно вернуть "I found Nemo at 0!"
 */

const solution = (sentence) => {
	return '';
};

module.exports = solution;