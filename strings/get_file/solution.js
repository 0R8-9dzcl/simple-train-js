/**
 * Сложность 0
 * Задача: Извлечь имя файла
 *
 * Напишите функцию, которая извлекает имя файла с расширением из переданного пути файловой системы.
 *
 * @param {string} filepath - Путь к файлу.
 * @returns {string} - Имя файла с расширением.
 *
 * Примеры:
 * solution("C:/Projects/tests/texts/something.txt")  // Должно вернуть "something.txt"
 * solution("C:/brain-games.exe")                     // Должно вернуть "brain-games.exe"
 * solution("C:/Users/JohnDoe/Music/Beethoven_5.mp3") // Должно вернуть "Beethoven_5.mp3"
 */

const solution = (filepath) => {
  return '';
};

module.exports = solution;
