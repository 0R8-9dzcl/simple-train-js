/**
 * Задача: Рассчитайте общее число возможных путей, которые может пройти продавец с данными n путями.
 *
 * @param {number} townCount - Количество городов и путей.
 * @returns {number} - Общее число возможных путей.
 *
 * Примеры:
 * solution(1);  // Должно вернуть 1
 * solution(4);  // Должно вернуть 24
 * solution(8);  // Должно вернуть 40320
 */

const solution = (townCount) => {
  return 1;
};

module.exports = solution;
