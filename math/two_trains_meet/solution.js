

// Задача: Расчет времени встречи поездов

// Условие задачи:
// Два поезда двигаются навстречу друг другу между двумя пунктами на прямом участке железнодорожного пути.
// Первый поезд движется со скоростью v1 километров в час, а второй поезд со скоростью v2 километров в час.
// Известно, что начальное расстояние между ними равно distance километрам.

// Вам необходимо рассчитать, через какое время после отправления поездов они встретятся друг с другом.
// Функция solution должна вернуть время в часах (с плавающей точкой), через которое они встретятся.

// Если сумма скоростей v1 и v2 равна нулю (т.е., оба поезда стоят на месте), то функция должна вернуть null, так как они никогда не встретятся.

// Примеры:
// solution(50, 50, 100); // Должно вернуть 1.0
// solution(30, 40, 140); // Должно вернуть 2.0
// solution(70, 50, 360); // Должно вернуть 3.0

/**
 * Рассчитывает время, через которое два поезда встретятся.
 * @param {number} v1 - Скорость первого поезда (километры в час).
 * @param {number} v2 - Скорость второго поезда (километры в час).
 * @param {number} distance - Начальное расстояние между поездами (километры).
 * @returns {number|null} Время встречи в часах или null, если поезда не встретятся.
 */

const solution = (v1, v2, distance) => {  
  return 0 || null;
};

module.exports = solution;
