/**
 * Задача: Сумма внутренних углов многоугольника
 *
 * Напишите функцию, которая рассчитывает сумму внутренних углов правильного n-стороннего многоугольника в градусах.
 *
 * @param {number} n - Количество сторон многоугольника.
 * @returns {number} - Сумма внутренних углов в градусах.
 *
 * Примеры:
 * solution(3); // Должно вернуть 180
 * solution(4); // Должно вернуть 360
 * solution(6); // Должно вернуть 720
 */

const solution = (n) => {
  return 0;
};

module.exports = solution;
