/**
 * Сложность -3
 * Задача: Рассчитать век
 *
 * Напишите функцию, которая рассчитывает век от полученного года.
 *
 * @param {number} year - Год.
 * @returns {number} - Век.
 *
 * Примеры:
 * solution(2005); // Должно вернуть 21
 * solution(1950); // Должно вернуть 20
 * solution(1900); // Должно вернуть 19
 * solution(2055); // Должно вернуть 21
 */

const solution = (year) => {
  return 0;
};

module.exports = solution;
