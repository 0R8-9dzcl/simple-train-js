/**
 * Задача: Рассчитайте сумму ASCII представления символов в строке.
 *
 * @param {string} word - Входная строка.
 * @returns {number} - Сумма ASCII представления символов в строке.
 *
 * Примеры:
 * solution("test");      // Должно вернуть 448
 * solution("clojure");   // Должно вернуть 756
 * solution("frontend");  // Должно вернуть 864
 */

const solution = (word = '') => {
  let result = 0;
  return result;
};

module.exports = solution;
