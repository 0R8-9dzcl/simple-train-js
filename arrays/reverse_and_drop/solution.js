/**
 * Сложность 1
 * Задача: Удаление и переворот слов
 *
 * Напишите функцию, которая удаляет слова с нечетной длиной в строке, а слова с четной длиной переворачивает.
 *
 * @param {string} sentence - Входная строка.
 * @returns {string} - Результат обработки строки.
 *
 * Примеры:
 * solution('learn clojure be happy'); // Должно вернуть 'eb'
 * solution('some test words with odd length'); // Должно вернуть 'emos tset htiw htgnel'
 */

const solution = (sentence) => {
  return '';
};

module.exports = solution;
