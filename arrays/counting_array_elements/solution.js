/**
 * Задача: Подсчет повторений элементов в массиве.
 *
 * @param {string[]} arr - Исходный массив.
 * @returns {Object} - Ассоциативный массив с количеством повторений элементов.
 *
 * Примеры:
 * solution(["cat","dog","fish","fish"]);                 // Должно вернуть {"cat":1,"dog":1,"fish":2}
 * solution(["Spam","egg","Spam","Spam","bacon","Spam"]); // Должно вернуть {"bacon":1,"egg":1,"spam":4}
 */

const solution = (arr) => {
  const result = {};
  return result;
};

module.exports = solution;
